# Install this file FIRST, then `pip install -e ".[dev]"` -- pip then resolves
# these exact versions rather than the looser floors in pyproject.toml, and the
# published numbers reproduce. This file mirrors pyproject's dependencies,
# pinned to the development environment. Requires Python 3.12+.
pandas==3.0.3
numpy==2.5.1
scikit-learn==1.9.0
xgboost==3.3.0
ucimlrepo==0.0.7
cryptography==50.0.0
joblib==1.5.3
matplotlib==3.11.0
pytest==9.1.1
